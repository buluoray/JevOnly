{
  "name": "jevonly-browser",
  "version": "0.1.0",
  "private": true,
  "description": "The Playwright browser bridge for JevOnly.",
  "scripts": {
    "test": "node --test tests/js/",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "dependencies": {
    "playwright": "1.63.0"
  },
  "devDependencies": {
    "eslint": "9.39.5",
    "prettier": "3.9.8"
  },
  "engines": {
    "node": ">=22"
  }
}
