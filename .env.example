# Required TypeSafe System One credential; never commit a real value.
TYPESAFE_API_KEY=tsk_your_key_here

# Optional viewer port for wrappers that pass `jevonly serve --port`.
JEVONLY_PORT=7791

# Optional Node Playwright module location when normal resolution is unavailable.
JEV_PLAYWRIGHT=node_modules/playwright

